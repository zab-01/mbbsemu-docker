services:
  mbbsemu:
    image: ghcr.io/zab-01/mbbsemu:latest
    container_name: mbbsemu
    ports:
      - "2323:23"
      - "1513:513"
    environment:
      TZ: America/Chicago
      PUID: "1000"
      PGID: "1000"
      SYSOP_PASSWORD: "testpass"
      # Wiki demo pair — replace with your own for production
      MUD_REG_NUMBER: "00053261"
      MUD_ACTIVATION_CODE: "bjh^^dl^^dl"
    volumes:
      - ./config:/config
    restart: unless-stopped
